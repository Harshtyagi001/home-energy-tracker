services:
  mysql:
    image: mysql:latest
    container_name: home-energy-tracker-mysql
    environment:
      MYSQL_DATABASE: home-energy-tracker
      MYSQL_USER: home-energy-tracker
      MYSQL_PASSWORD: home-energy-tracker
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - ./docker/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - db-data:/var/lib/mysql
      - db-data:/var/log/mysql

volumes:
    db-data: